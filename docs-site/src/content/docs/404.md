---
title: Page not found
description: 'That page is not here. The rowshape docs cover install, the CLI reference, the finding catalog, the fixture spec, and the agent and MCP surface.'
template: splash
editUrl: false
# A request for a path that does not exist gets a real 404 from Cloudflare
# Pages. But the page ALSO lives at its own URL — /404 returns HTTP 200, and
# /404.html 308-redirects to it — which is a soft 404: an indexable page whose
# entire content is "this is not a page". Verified against the live site.
#
# noindex is the remedy for exactly that. `follow` is deliberate: the links
# below point into the five sections of the docs, and there is no reason to
# throw those away.
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex, follow
sidebar:
  hidden: true
---

The page you asked for does not exist — it may have moved, or the link that
brought you here may be out of date.

- [Install](/install/) — a single static binary, no runtime
- [CLI reference](/reference/) — every command and flag, generated from the binary
- [Finding catalog](/findings/) — every code rowshape can return, and its remediation
- [Fixture spec](/spec/) — RFC-0001, the format rowshape reads and writes
- [Agents & MCP](/agent/) — putting the verdict inside an agent's turn

If a rowshape page used to be at this URL, please
[open an issue](https://github.com/rowshape/rowshape/issues) — a docs link that
rotted is a bug.
